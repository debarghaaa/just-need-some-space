import { LoadingScreen } from '@/components/site/LoadingScreen';

/** Route-level loading UI: the single loading word while this segment streams in. */
export default function Loading() {
  return <LoadingScreen />;
}
